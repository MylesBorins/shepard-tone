const oscillators=[];const step=.1;const MIN_FREQ=440;const MAX_FREQ=880;function makeOscillators(context,gain){for(var i=0;i<4;i++){var osc=context.createOscillator();osc.type="square";osc.frequency.value=440+i*30;osc.connect(gain);osc.start();oscillators.push(osc)}}function stepOscillators(steps=1){oscillators.forEach((osc=>{osc.frequency.value=osc.frequency.value+step*steps;if(osc.frequency.value>MAX_FREQ)osc.frequency.value=MIN_FREQ+(osc.frequency.value-MAX_FREQ)}))}function initAudio(){var context=new(window.AudioContext||window.webkitAudioContext);var gain=context.createGain();gain.gain.value=.6;gain.connect(context.destination);makeOscillators(context,gain);return context}export{initAudio,stepOscillators};